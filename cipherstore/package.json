{
  "name": "@vulnseal/cipherstore",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "backup": "node dist/backup.js",
    "dev": "npm run build && node ../scripts/cipherstore-dev.mjs",
    "test": "vitest --maxWorkers=2",
    "test:run": "vitest run --maxWorkers=2",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
