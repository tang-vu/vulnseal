# Copy outside Git or use a local env file. These are public deployment values.
# Use an image digest, e.g. registry.example/vulnseal@sha256:..., or a local sha256 image ID.
WEB_RELEASE_A_IMAGE=
WEB_RELEASE_B_IMAGE=
WEB_RELEASE_A=release-a
WEB_RELEASE_B=release-b
WEB_ACTIVE_RELEASE=release-a
WEB_PUBLISHED_PORT=8080
