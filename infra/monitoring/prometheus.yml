# SPDX-License-Identifier: Apache-2.0
global:
  scrape_interval: 15s
  scrape_timeout: 5s
  evaluation_interval: 15s
rule_files:
  - /etc/prometheus/cipherstore-rules.yml
scrape_configs:
  - job_name: cipherstore
    static_configs:
      - targets: ["cipherstore:8787"]
